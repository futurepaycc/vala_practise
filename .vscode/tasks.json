{
	// See https://go.microsoft.com/fwlink/?LinkId=733558
	// for the documentation about the tasks.json format
	"version": "2.0.0",
	"tasks": [
		{
			"label": "recompile",
			"type": "shell",
			"command": "rm -f ${workspaceRoot}/bin/${fileBasenameNoExtension} && make source=${fileBasenameNoExtension}",
			"problemMatcher": []
		}
	]
}